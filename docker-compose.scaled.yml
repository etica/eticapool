version: "3.8"

services:
  redis:
    image: redis:7-alpine
    command: >
      redis-server
      --appendonly yes
      --maxmemory 512mb
      --maxmemory-policy noeviction
    # Redis port not exposed to host; only accessible to services via internal network
    volumes:
      - redis-data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3

  token-collector:
    build: .
    command: ["node", "index-token-collector.js"]
    depends_on:
      redis:
        condition: service_healthy
    environment:
      - MONGODB_URI=mongodb://host.docker.internal:27017
      - REDIS_URL=redis://redis:6379
      - POOL_ENV=production
      - POOL_CONFIG_PATH=/app/config/pool.config.json
    volumes:
      - ./pool.config.json:/app/config/pool.config.json:ro
      - ./log:/app/log
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      disable: true
    restart: unless-stopped

  share-processor:
    build: .
    command: ["node", "index-share-processor.js"]
    depends_on:
      redis:
        condition: service_healthy
    environment:
      - MONGODB_URI=mongodb://host.docker.internal:27017
      - REDIS_URL=redis://redis:6379
      - POOL_ENV=production
      - POOL_CONFIG_PATH=/app/config/pool.config.json
    volumes:
      - ./pool.config.json:/app/config/pool.config.json:ro
      - ./log:/app/log
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      disable: true
    restart: unless-stopped

  stratum:
    build: .
    command: ["node", "index-stratum.js"]
    depends_on:
      redis:
        condition: service_healthy
    environment:
      - MONGODB_URI=mongodb://host.docker.internal:27017
      - REDIS_URL=redis://redis:6379
      - POOL_ENV=production
      - POOL_CONFIG_PATH=/app/config/pool.config.json
    ports:
      - "3333:3333"
      - "5555:5555"
      - "7777:7777"
      - "9999:9999"
      - "8081:8081"
    volumes:
      - ./pool.config.json:/app/config/pool.config.json:ro
      - ./log:/app/log
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      disable: true
    restart: unless-stopped

  web:
    build: .
    command: ["node", "index-web.js"]
    depends_on:
      redis:
        condition: service_healthy
    environment:
      - MONGODB_URI=mongodb://host.docker.internal:27017
      - REDIS_URL=redis://redis:6379
      - POOL_ENV=production
      - POOL_CONFIG_PATH=/app/config/pool.config.json
    ports:
      - "80:8080"
      - "2053:2053"
    volumes:
      - ./pool.config.json:/app/config/pool.config.json:ro
      - ./log:/app/log
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

volumes:
  redis-data:
